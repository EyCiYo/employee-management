const Navbar = () => {
    return (
        <nav>
            <img
                src="src/assets/kv logo.png"
                alt="logo"
                height="50px"
                style={{
                    margin: "0.5rem",
                    marginLeft: "2rem",
                }}
            />
        </nav>
    );
};

export default Navbar;
